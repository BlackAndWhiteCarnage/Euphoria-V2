/**
 * Internal dependencies
 */
import { ProductLayout, WithSliderLayout } from 'layouts';

const Product = () => (
	<WithSliderLayout>
		<ProductLayout />
	</WithSliderLayout>
);

export default Product;
